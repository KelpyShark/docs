# Reading from Standard Input

Use `std.input(prompt)` to read a line of text from the user.

## Example
```kelpyshark
line = std.input("Type something: ")
print "You typed: " + line
```

## Summary
- `std.input` reads a line from the user.