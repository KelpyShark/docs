# Deleting Files

KelpyShark's standard library provides a function to delete files.

## Example
```kelpyshark
std.delete_file("old.txt")
```

## Summary
- Use `std.delete_file(filename)` to remove a file from disk.